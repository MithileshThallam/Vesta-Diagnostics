{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",  // Entry point (adjust if needed)
      "use": "@vercel/node"   // Uses Node.js runtime
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" } // Routes API requests
  ]
}
